from django.contrib import admin
from django.urls import path
from . import views


urlpatterns = [
    # basic layout: url(r'^...(leave blank for default)...$', views.viewname), or path('',views.viewname),
]
