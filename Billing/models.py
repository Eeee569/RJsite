from django.db import models

# #todo: turn content into a pdf or possibly a billing service
#
# class Bill(models.Model):
#     billingTeacher = models.ForeignKey('usr.Teacher',on_delete=models.SET_NULL,null=True)
#     billingParent = models.ForeignKey('usr.Parent',on_delete=models.SET_NULL,null=True)
#     content = models.CharField(max_length=1000)
# # Create your models here.
#
