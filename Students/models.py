from django.db import models
from usr.models import Student
from CollageApp.models import StudentEssay
# Create your models here.
class StudentProgress(models.Model):
    pass
    #actualy can just build from teacher object listing all students and building info
